name: Release

on:
  push:
    branches: [main]

env:
  VERSION: v1

jobs:
  tag:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: git tag -d $VERSION
      - run: git tag $VERSION
      - run: git push origin :$VERSION
      - run: git push --tag
